// Google OAuth Web Client ID — same across all backend environments (local/int/prod)
// since it identifies the Firebase/Google Cloud project, not which API we're calling.
// Safe to keep in source: this is a public client identifier, not a secret.
export const GOOGLE_WEB_CLIENT_ID = '225208963602-i9fgjukio4ue22ffjjmr910k70cbfarm.apps.googleusercontent.com';
